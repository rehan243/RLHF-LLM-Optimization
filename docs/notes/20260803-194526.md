## Note on preference optimization

A recurring lesson working on reward modeling, PPO/DPO, and KL control: the failure mode is almost never where the intuition points. Measuring the change end-to-end — not reasoning about it in isolation — is what surfaces the real bottleneck. Documenting it here so the next decision starts from evidence rather than a guess.
